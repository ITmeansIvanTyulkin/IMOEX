package com.moex.trinity.trend;

public enum LimitGridStyle {
    /** Even split across near / mid / far (e.g. 2-2-2). */
    MODERATE,
    /** Near-heavy: (N-2)-1-1 (e.g. 3-1-1 or 4-1-1). */
    AGGRESSIVE
}
